<!DOCTYPE html>
<html>
  <head>
    <title>IP Information</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" value="#f2f2f2" />
    <link rel="icon" href="favicon.ico" />
    <style nonce="{{ nonce }}">
      {{ style }}
    </style>
  </head>
  <body>
    <h1>IP Information</h1>

    <dl>
      <dt>IP</dt>
      <dd>{{ ipDisplay }}</dd>

      {% if infodata.country %}
      <dt>Country</dt>
      <dd>{{ infodata.country }}</dd>
      {% endif %}

      {% if infodata.countryCode %}
      <dt>Country code</dt>
      <dd>{{ infodata.countryCode }}</dd>
      {% endif %}

      <dt>ASN</dt>
      <dd>{{ infodata.asn }}</dd>

      {% if infodata.isp %}
      <dt>ISP</dt>
      <dd>{{ infodata.isp }}</dd>
      {% endif %}

      <dt>User-Agent</dt>
      <dd>{{ infodata.userAgent }}</dd>
    </dl>

    <footer>
      <p>
        <!-- prettier-ignore-start -->
        Created by
        <a href="https://rensatsu.netlify.app/" target="_blank" rel="noopener">Rensatsu</a>,
        {{ year }}.

        <a href="https://github.com/rensatsu/my-ip-worker" target="_blank" rel="noopener">Source code</a>.
        <!-- prettier-ignore-end -->
      </p>

      <p class="debug">
        <time datetime="{{ timestamp }}">{{ datetime }}</time>
      </p>
    </footer>
  </body>
</html>
